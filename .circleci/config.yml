version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1-jessie
    steps:
      - checkout
      - run:
          name: install-npm
          command: 'npm install'
      - run: 
          name: install-npm
          command: 'npm run build'
  